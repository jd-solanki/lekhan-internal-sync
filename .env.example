# Check `shared/libs/env.ts` for programmatic validation & defaults
# Values having pre-defaults in `.env.example` means when you create `.env` those are likely values & shouldn't be changed
# INFO: precommit hook is configured to auto-update `.env.example` from `.env` ignoring secrets
#   If you want to add pre-defaults add them in `.env.example` manually. Our precommit hook won't override pre-default values in `.env.example`

# NOTE: If you are deploying to Vercel, then use www subdomain in .env.prod because Vercel recommends having www subdomain for their CDN. If the non-www domain is used, Vercel automatically redirects it to www subdomain.
# When the app performs redirects using the non-www value, this results in an additional redirect to the www URL, creating unnecessary redirect chains and inconsistent routing.
# Using www directly in environment variables prevents this extra redirect.
# ------------------------------------------------------------------------------------------------------------------------------

NODE_ENV=

# App global variables
NUXT_PUBLIC_APP_NAME=
NUXT_PUBLIC_APP_DOMAIN=
NUXT_PUBLIC_APP_BASE_URL=

# 💬 Contact Us
NUXT_PUBLIC_CONTACT_EMAIL=

# 💾 Database
# Main database connection URL used by the app at runtime.
# If your hosting provider offers a connection pooler (e.g. PgBouncer),
# use that URL here — it manages connections efficiently, especially in
# serverless environments.
#
# Supabase:  Settings → Database → Transaction pooler (port 6543)
# Vercel PG: POSTGRES_URL (auto-set)
# Self-hosted / simple setups: just your regular postgres connection string
#
DATABASE_URL=postgres://<username>:<password>@localhost:5432/<database_name>

# Direct database connection used ONLY for running migrations.
# Migrations require a direct connection — they cannot run through a pooler.
#
# If you don't use a connection pooler (self-hosted, simple setups),
# leave this unset — it will fall back to DATABASE_URL automatically.
#
# Supabase:  Settings → Database → Direct connection (port 5432)
# Vercel PG: POSTGRES_URL_NON_POOLING (auto-set)
# Self-hosted: leave unset
#
DIRECT_DATABASE_URL=

# ✉️ Email (Any one provider required to be configured for production)
#supported-email-services
# In dev env it'll be default to SMTP at localhost:1025
# You can refer to `layers/email/server/utils/email.ts` for more details
RESEND_API_KEY=

# 💾 Storage
# Prefix `APP_` to avoid conflict with third party services like Vercel/Netlify which uses `AWS_` prefixed env vars
APP_AWS_ACCESS_KEY=
APP_AWS_SECRET_KEY=
APP_AWS_REGION=
APP_AWS_BUCKET_NAME=

# 🧙🏻‍♂️ Admin
# For receiving unexpected errors and alerts
# Use CSV format to add multiple emails
ADMIN_EMAILS=

# 🔒 BetterAuth
# You can generate a random secret using `openssl rand -base64 32`
BETTER_AUTH_SECRET=

# It should have same value as `NUXT_PUBLIC_APP_BASE_URL`
BETTER_AUTH_URL=

# Prefix `AUTH_` because GitHub secrets don't allow secret having `GITHUB_` prefix
AUTH_GOOGLE_CLIENT_ID=
AUTH_GOOGLE_CLIENT_SECRET=
AUTH_GITHUB_CLIENT_ID=
AUTH_GITHUB_CLIENT_SECRET=

# 💰 Polar
# Dashboard URL link will be available in admin navigation for quick access
POLAR_DASHBOARD_URL=
POLAR_ACCESS_TOKEN=
POLAR_SERVER=
POLAR_WEBHOOK_SECRET=
POLAR_CHECKOUT_FOR_AUTHENTICATED_USERS_ONLY=

# 🕒 Cron
# You can generate a random secret using `openssl rand -base64 32`
CRON_SECRET=
